export const VFX = {
  playHitEffect(tokenElement, damageType = 'physical') {
    console.log(`[PLACEHOLDER] Playing hit effect: ${damageType} on`, tokenElement);
  },

  playDeathEffect(tokenElement, deathType = 'enemy') {
    console.log(`[PLACEHOLDER] Playing death effect: ${deathType} on`, tokenElement);
  },

  playStatusEffect(tokenElement, statusType = 'poison') {
    console.log(`[PLACEHOLDER] Playing status effect: ${statusType} on`, tokenElement);
  }
};
